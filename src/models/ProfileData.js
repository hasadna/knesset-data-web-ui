export class ProfileData {
	constructor({name, nameUrl, title, titleUrl, picUrl}) {
		this.name = name;
		this.nameUrl = nameUrl;
		this.title = title;
		this.titleUrl = titleUrl;
		this.picUrl = picUrl;
	}
}
